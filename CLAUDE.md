# Repository context

The binding foundation is `../base/CONSTITUTION.md`. Then follow `BIBLE.md`, the family contract
in `../razbiram-nlp/docs/razbiram-ECOSYSTEM.md`, `AGENTS.md`, and the newest `HANDOFF.md` entry.

This repository is currently documentation-first. Do not begin broad implementation until the M0
acceptance criteria in `docs/ROADMAP.md` are approved.

Start with `./scripts/state.sh` and `./scripts/gate.sh`. End with a newest-first HANDOFF entry,
durable BIBLE decisions, a green gate, granular Conventional Commits, push, and
`./scripts/secure.sh`.
